import 'package:flutter/material.dart';

class BookBusPage extends StatelessWidget {
  const BookBusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Book Bus')),
      body: const Center(child: Text('Book Bus - placeholder')),
    );
  }
}
